[GEOLOCATE]
# Data warehouse source tables and target SDE features
# Each entry: [dw_table_name, target_sde_feature_name]
dw_source_tables = [["OPENDATA_SOURCE.PPLC_PLANNING_APPLICATIONS", "SDEADM.LND_PPLC_planning_applications"], ["OPENDATA_SOURCE.PPLC_SUBDIVISION_APPLICATIONS", "SDEADM.LND_PPLC_subdivision_applications"]]
# Default PID field name used for any table not listed in [PID_FIELDS]
pid_field = PID
truncate_and_load = True

[PID_FIELDS]
# Per-table PID field name overrides.
# Key = DW source table name, Value = PID field name in that table.
# Tables not listed here fall back to pid_field in [GEOLOCATE].
# If a table has no PID field, omit it from both sections and it will be skipped.
OPENDATA_SOURCE.PPLC_PLANNING_APPLICATIONS = PIDs
